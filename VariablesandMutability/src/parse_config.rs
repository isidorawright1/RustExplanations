/*
Scenario: Parse a simple string representing a node configuration for an AI platform cluster, String format is "hostname;cpu_cores;has_gpu".
If any part of the parse fails, return a specific error.

Using these concepts: Result, Option, the ? operator, custom error enums, and string parsing
*/